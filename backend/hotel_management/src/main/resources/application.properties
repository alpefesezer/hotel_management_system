# Specify the Hibernate dialect for MySQL
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQLDialect

# Set the hibernate.hbm2ddl.auto property to update
spring.jpa.hibernate.ddl-auto = update

# Configure the data source URL, username, password, and driver class
spring.datasource.url = jdbc:mysql://localhost:3306/hotel_management?useSSL=false&serverTimezone=UTC
spring.datasource.username = root
spring.datasource.password = Farsak38
spring.datasource.driver-class-name = com.mysql.cj.jdbc.Driver

# Additional configuration properties
spring.jpa.show-sql = true
spring.jpa.properties.hibernate.format_sql = true
